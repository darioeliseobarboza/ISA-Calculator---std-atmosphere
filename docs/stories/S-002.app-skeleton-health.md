---
id: S-002
title: "Esqueleto atmosphere-app + pantalla de health"
status: Ready
created: 2026-06-15
source: REQ-001
affects_ui: false
---

# S-002: Esqueleto atmosphere-app + pantalla de health

**Status:** Ready
**Created:** 2026-06-15
**Source:** REQ-001

> `affects_ui: false` — la única UI de esta story es la pantalla **provisional** de health (liveness); el REQ es `ux_review: not-applicable`. La UI real (calculadora) llega en FG-2.

## Historia de Usuario

**Como** usuario técnico,
**quiero** abrir `atmosphere-app` (Windows/Linux/Web) y ver que la API está disponible,
**para** confirmar que el sistema está vivo end-to-end.

## Criterios de Aceptación

### CA-1: App multiplataforma corre

**DADO** el código de `atmosphere-app`
**CUANDO** se buildea/ejecuta para Windows, Linux y Web
**ENTONCES** los tres targets compilan y la app arranca

### CA-2: Pantalla de health muestra estado disponible

**DADO** `atmosphere-api` corriendo y `API_BASE_URL` configurada
**CUANDO** abro la app
**ENTONCES** consulta `GET /health` y muestra que la API está **disponible** ("sistema vivo")

### CA-3: Estado de error de conexión

**DADO** la app corriendo
**CUANDO** la API no responde / no hay conectividad / status ≥ 400
**ENTONCES** la app muestra un **estado de error de conexión** (no "vivo")

### CA-4: Web cross-origin

**DADO** el target Web servido en un origen y la API en otro
**CUANDO** la app web llama a `/health`
**ENTONCES** la llamada funciona (CORS habilitado en la API), sin error de CORS

## Diseño Técnico

### Servicios Afectados

| Servicio | Impacto | Cambios Necesarios | Estado |
|----------|---------|-------------------|--------|
| atmosphere-app | Alto | Esqueleto Flutter (3 targets), `ApiClient` (http) + repository, env `API_BASE_URL`, pantalla provisional de health, Dockerfile web + docs build escritorio | Pending |

Convenciones (ver `docs/architectures/atmosphere-app/`): `networking` (http), `state-management` (Riverpod), `env-config` (flutter_dotenv), `theming`, `navigation`, `error-handling` (auto), `_base`. (`i18n`/`models-serialization`/`testing` aplican más en FG-2.)

### Cambios de API

#### Endpoints Existentes a Modificar
Ninguno.

#### Nuevos Endpoints
Ninguno (la app **consume** `GET /health`, ya especificado).

### Cambios de Base de Datos
Ninguno.

### Flujo de Interacción

#### Escenario: Sistema vivo (health check end-to-end)

**Trigger:** el usuario abre `atmosphere-app`.

**Flujo:**
1. **[atmosphere-app]** `ApiClient` (http) → `GET {API_BASE_URL}/health`.
2. **[atmosphere-api]** → `200 { "status": "ok", "timestamp": ... }`.
3. **[atmosphere-app]** muestra "API disponible".

**Outcome:** el usuario ve el sistema vivo end-to-end en cualquiera de los 3 targets.

**Manejo de errores:**
- Timeout / sin red / status ≥ 400 → pantalla muestra **error de conexión**.

### Flujos Afectados

- Ninguno documentado se modifica (health-check trivial).

### Consideraciones Técnicas

**Seguridad:** sin auth; la app solo lee `/health`.
**Performance:** ping liviano al abrir.
**Dependencias:** requiere `GET /health` de S-001 y `API_BASE_URL` (env).

## Dependencias

**Prerequisitos:**
- S-001 (necesita el `/health` de la API).

**Habilita:**
- S-003 (la orquestación Docker levanta esta app + la API juntas).

**Externas:**
- Ninguna.

---

**Origen:** [REQ-001](../requests/REQ-001.walking-skeleton.md)
