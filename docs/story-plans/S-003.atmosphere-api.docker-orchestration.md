# Story Plan: S-003 (atmosphere-api) - Orquestación Docker (compose dev/prod)

## Status

**Status:** Ready

## Story Reference

- **Story:** [S-003](../stories/S-003.docker-orchestration.md)
- **Service:** atmosphere-api
- **Summary:** Crear la orquestación Docker a nivel monorepo (`docker/docker-compose.yml` base + `docker-compose.dev.yml` + `docker-compose.prod.yml`) que levanta los DOS servicios (`atmosphere-api` y `atmosphere-web`) con paridad estructural base+override, dejando el "sistema vivo" end-to-end en dev y el compose de producción detrás de `ingress-network`.

> **Nota de alcance (orquestación monorepo).** Aunque este plan está scopeado al servicio `atmosphere-api`, el entregable de S-003 es **infraestructura a nivel raíz del repo** (`docker/`) que cubre AMBOS servicios. Por eso el plan incluye también el servicio `atmosphere-web` (build de `atmosphere-app`). Los **Dockerfiles** de cada servicio ya existen (S-001 para la API, S-002 para la web) y **no** se modifican en esta story salvo verificación; lo nuevo son los tres archivos compose + el README de `docker/`.

## Architectural Context

### Tech Stack

| Componente | Tecnología / Versión |
|---|---|
| `atmosphere-api` (runtime) | Binario Go 1.26.4 (stdlib `net/http`), imagen base `gcr.io/distroless/static-debian12:nonroot` (sin shell, sin gestor de paquetes, non-root) |
| `atmosphere-web` (runtime) | Build de Flutter web servido por `nginx:1.27-alpine`. Builder pineado a `ghcr.io/cirruslabs/flutter:3.44.0` (el SDK local es 3.44.1 pero cirruslabs no publica imagen prebuilt para ese parche; 3.44.0 es la última stable publicada del mismo minor y satisface el constraint de pubspec). Corregido en S-003 al verificar el build end-to-end. |
| Orquestación | Docker Compose v2 (sintaxis `docker compose`, patrón base + overrides con `-f`) |
| Proxy (prod, externo) | nginx-proxy + acme-companion sobre red externa `ingress-network` |

**Estructura del monorepo (objetivo tras esta story):**

```
/ (raíz)
├── docs/
├── services/
│   ├── atmosphere-api/     # Go — Dockerfile ya existe (S-001)
│   └── atmosphere-app/     # Flutter — Dockerfile (target web→nginx) ya existe (S-002)
└── docker/                 # NUEVO en esta story
    ├── docker-compose.yml        # base (común)
    ├── docker-compose.dev.yml    # override local
    ├── docker-compose.prod.yml   # override producción
    └── README.md                 # prerequisitos (ingress-network, proxy) + comandos
```

### Contrato real de cada servicio (verificado en el código, autoritativo)

**`atmosphere-api`** — variables de entorno que efectivamente lee (`cmd/atmosphere-api/config.go`, vía `loadConfig`):

| Variable | Default | Notas |
|---|---|---|
| `ENV` | `development` | `"development"` \| `"production"`. En `production`, `CORS_ALLOWED_ORIGINS` es obligatoria (fail-fast). |
| `HTTP_ADDR` | `:8080` | Dirección de escucha host:port. **Este es el nombre real**, NO `PORT`. |
| `LOG_DIR` | `./logs` | Directorio de logs estructurados (slogx). |
| `LOG_DEBUG` | `false` | `"true"` habilita debug. |
| `CORS_ALLOWED_ORIGINS` | `""` | Lista separada por comas, sin wildcard. Requerida en `production`. |

> **DISCREPANCIA DOCUMENTADA — needs awareness, no bloquea.** ADR-004 y `docs/prd/architecture.md` mencionan la variable de la API como **`PORT`**, pero la implementación real (S-001) usa **`HTTP_ADDR`** (default `:8080`). Este plan usa el contrato real del código (`HTTP_ADDR`). Las demás variables del ADR (`ENV`, `LOG_DIR`, `LOG_DEBUG`) tampoco aparecían en el ADR pero existen en el binario. Los compose deben declarar `HTTP_ADDR`, no `PORT`.

- Puerto interno del contenedor API: **8080** (`EXPOSE 8080` en el Dockerfile, alineado con el default `HTTP_ADDR=:8080`).
- Comportamiento CORS (`internal/http/middleware.go`): solo los orígenes presentes en `CORS_ALLOWED_ORIGINS` reciben el header `Access-Control-Allow-Origin: <origin>` (eco del origin) + `Access-Control-Allow-Methods: GET, POST, OPTIONS` + `Access-Control-Allow-Headers: Content-Type` + `Vary: Origin`. Preflight (`OPTIONS` con `Access-Control-Request-Method`) desde origen permitido → `204`; desde origen no permitido → `403`. Sin wildcard.
- **Constraint imagen distroless:** la imagen `atmosphere-api` NO tiene shell ni `curl`/`wget`. Un `healthcheck` de compose basado en shell (`CMD-SHELL "curl ..."`) **NO funciona** en este contenedor. El binario tampoco expone un subcomando de healthcheck. Por eso el `healthcheck` del servicio API en el base se documenta como **deshabilitado/omitido** (o, si se quisiera, requeriría agregar un healthcheck-binario al Dockerfile — fuera de alcance de S-003). El endpoint `GET /health` sigue disponible para chequeo externo (proxy / verificación manual / la web).

**`atmosphere-web`** (build del target web de `atmosphere-app`, `services/atmosphere-app/Dockerfile`):

| Variable / Arg | Tipo | Default | Notas |
|---|---|---|---|
| `API_BASE_URL` | **build-arg** (`ARG`) | `http://localhost:8080` | Se hornea en el bundle de Flutter en **build-time** (`flutter build web` escribe `.env` con `flutter_dotenv`). **NO es una env var de runtime.** |

> **CONSTRAINT CLAVE — `API_BASE_URL` es build-time, no runtime.** En el contenedor `atmosphere-web` el bundle de Flutter ya tiene la URL horneada. Consecuencias para los compose:
> - **dev** (override usa `build:`): se pasa `args: { API_BASE_URL: http://localhost:8080 }` bajo `build`. Funciona porque dev buildea localmente.
> - **prod** (override usa `image:` del registry): la imagen YA fue construida con su `API_BASE_URL` (el dominio de la API) al momento de publicarla al registry. El compose de prod **no** puede inyectar `API_BASE_URL` por env de runtime; debe asumir que la imagen versionada se construyó con el `API_BASE_URL` productivo. Se documenta como prerequisito de publicación de imagen.

- Puerto interno del contenedor web: **80** (`EXPOSE 80`, nginx `listen 80`).
- `nginx.conf` ya hace fallback SPA (`try_files $uri $uri/ /index.html`) para go_router; `index.html` con `Cache-Control: no-cache`.

### Relevant ADRs

#### ADR-004: Estrategia de deployment (monorepo + Docker dev/prod + ingress-network)

- **Decisión:** Monorepo con `services/atmosphere-api`, `services/atmosphere-app` y `docker/`. Compose con patrón **base + overrides**: `docker-compose.yml` (base común, única fuente de verdad de lo compartido), `docker-compose.dev.yml` (build desde el path, puertos en localhost), `docker-compose.prod.yml` (imágenes del registry, red externa `ingress-network`, sin puertos al host). Contenedores: `atmosphere-api` (binario Go) y `atmosphere-web` (Flutter web→nginx); desktop = binarios nativos NO contenerizados. Web y API son orígenes distintos → CORS habilitado. Proxy = nginx-proxy + acme-companion (enruta por `VIRTUAL_HOST`/`VIRTUAL_PORT`, TLS por `LETSENCRYPT_HOST`).

- **Implementation Rules (verbatim — constraints obligatorios):**
  - Estructura monorepo MUST ser: `services/atmosphere-api/`, `services/atmosphere-app/`, `docker/docker-compose.yml` (base), `docker/docker-compose.dev.yml` (override dev), `docker/docker-compose.prod.yml` (override prod).
  - `docker-compose.yml` (base) MUST contener la definición común de los servicios (nombres, env vars comunes, healthcheck, alias de red) — única fuente de verdad de lo compartido. NO define `build`/`image`/`ports`/red de entorno (eso va en los overrides).
  - El override **dev** MUST agregar `build:` desde el path del proyecto y exponer puertos en localhost (p. ej. API `8080`, web `8081`).
  - El override **prod** MUST agregar `image:` desde el registry oficial (tags versionados), conectar los servicios a la red externa **`ingress-network`** y NO exponer `ports` al host.
  - En prod, cada servicio expuesto MUST definir: `VIRTUAL_HOST` (dominio), `VIRTUAL_PORT` (puerto interno del contenedor) y `LETSENCRYPT_HOST` (= `VIRTUAL_HOST`).
  - Los comandos MUST combinar base + override: dev → `docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml up`; prod → `docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml up -d`.
  - **Paridad:** lo común MUST vivir una sola vez en el base (paridad estructural); cada override carga SOLO sus diferencias (`build` vs `image`, red, puertos). NO duplicar la config común en los overrides.
  - `atmosphere-web` MUST ser un contenedor nginx que sirve el build de Flutter web; los targets desktop (Windows/Linux) MUST ser binarios nativos (`flutter build windows`/`linux`), NO contenerizados.
  - CORS MUST estar habilitado en `atmosphere-api` con orígenes permitidos vía `CORS_ALLOWED_ORIGINS` por ambiente.
  - Variables de entorno: `atmosphere-api` → `PORT`, `CORS_ALLOWED_ORIGINS`; `atmosphere-web` → `API_BASE_URL`; en prod además `VIRTUAL_HOST`, `VIRTUAL_PORT`, `LETSENCRYPT_HOST` por servicio + registry/tags.
    - **Aclaración del implementor:** donde el ADR dice `PORT` para la API, el binario real usa `HTTP_ADDR` (ver "Contrato real" arriba). Usar `HTTP_ADDR`.

#### ADR-001: Stack y arquitectura cliente-servidor (contexto)

- API Go + frontend Flutter como servicios separados; comunicación HTTP/REST JSON síncrona. Relevante porque fija los dos contenedores que el compose orquesta.

#### ADR-003: Servicio stateless sin persistencia (contexto)

- `atmosphere-api` es stateless, sin base de datos. **Implicación para el compose:** no hay servicio de base de datos ni volúmenes de datos; el compose orquesta solo los dos contenedores de aplicación, sin `depends_on` hacia una DB.

### API Context

Único endpoint relevante para esta story (verificación end-to-end del sistema vivo):

- `GET /health` → `200` con cuerpo JSON `{ "status": "ok", "timestamp": <RFC3339> }` (de `docs/apis/atmosphere-api.yaml`).
- El endpoint `POST /v1/calculate` existe en el contrato pero NO se ejercita en S-003 (S-003 solo levanta el sistema y valida `/health` + CORS).
- Server local: `http://localhost:8080` (declarado en el OpenAPI como "Desarrollo local (docker-compose.dev.yml)").

CORS (resumen del contrato implementado, ver "Contrato real"): respuesta a un `GET /health` con header `Origin: http://localhost:8081` permitido → incluye `Access-Control-Allow-Origin: http://localhost:8081` y `Vary: Origin`.

### Database Context

Ninguno. `atmosphere-api` es stateless (ADR-003); el compose no incluye base de datos ni volúmenes de persistencia.

### Flow Context

La story declara que ningún flujo documentado (`docs/flows/calculate.md`) se modifica. El flujo operativo nuevo es el de "levantar el sistema en dev", descrito en la propia story:

1. **[compose dev]** build de `atmosphere-api` y `atmosphere-web` desde el path del proyecto; puertos en localhost (api `8080`, web `8081`).
2. **[atmosphere-web]** sirve el build de Flutter web; llama a la API por `API_BASE_URL` (build-arg `http://localhost:8080`).
3. **[atmosphere-api]** responde `/health` con CORS para el origen local (`http://localhost:8081`).

**Outcome:** sistema vivo end-to-end en local; mismo armado que prod salvo build/red/puertos.

### Integration Points

- **Red externa `ingress-network` (solo prod):** red Docker externa preexistente en el servidor, donde corren nginx-proxy + acme-companion. El compose de prod la referencia como `external: true`; NO la crea. Prerequisito de host (documentar en `docker/README.md`).
- **nginx-proxy + acme-companion (solo prod):** descubre contenedores por las env vars `VIRTUAL_HOST` (dominio público), `VIRTUAL_PORT` (puerto interno del contenedor a enrutar) y `LETSENCRYPT_HOST` (= `VIRTUAL_HOST`, dispara emisión/renovación TLS). Termina TLS; los contenedores NO exponen puertos al host en prod.
- **Registry oficial (solo prod):** imágenes versionadas de `atmosphere-api` y `atmosphere-web`. Los tags/repositorio se parametrizan vía variables (`API_IMAGE`, `WEB_IMAGE` o equivalentes). La imagen de `atmosphere-web` debe haberse construido con el `API_BASE_URL` productivo (build-time).

### Code Conventions

- Archivos compose en `docker/`, nombres exactos: `docker-compose.yml`, `docker-compose.dev.yml`, `docker-compose.prod.yml` (Implementation Rules ADR-004).
- Rutas de `build.context` en los overrides relativas a la carpeta `docker/`: `../services/atmosphere-api` y `../services/atmosphere-app`.
- Nombres de servicio compose: `atmosphere-api` y `atmosphere-web` (el contenedor web NO se llama `atmosphere-app`; ese es el repo/servicio fuente — el contenedor es `atmosphere-web`).
- No duplicar config común en los overrides (paridad estructural): nombres de servicio, env vars comunes y alias de red viven solo en el base.

### Reusable Code

Story de infraestructura (sin código de aplicación nuevo). Elementos relevantes existentes:

- **CORS middleware** (`atmosphere-api`, `internal/http/middleware.go`) — ya implementado (S-001); el compose solo provee el valor de `CORS_ALLOWED_ORIGINS` por ambiente. No se modifica.
- **`nginx.conf`** (`services/atmosphere-app/nginx.conf`) — fallback SPA ya configurado (S-002); reusado tal cual por el contenedor web. No se modifica.
- **Dockerfiles** existentes (`services/atmosphere-api/Dockerfile`, `services/atmosphere-app/Dockerfile`) — reusados por los overrides (dev `build`, prod `image` construida desde el mismo Dockerfile). No se modifican en esta story.

## Acceptance Criteria Coverage

| AC# | Descripción | Tarea(s) |
|---|---|---|
| CA-1 | Entorno local con docker-compose levanta API + web, web alcanza `/health` | T1, T2, T5 |
| CA-2 | CORS end-to-end en dev (web localhost → `/health` API) | T2, T5 |
| CA-3 | Compose de prod: imágenes del registry, red `ingress-network`, sin puertos al host, `VIRTUAL_HOST`/`VIRTUAL_PORT`/`LETSENCRYPT_HOST` por servicio expuesto | T3, T4 |
| CA-4 | Paridad base + overrides: común una sola vez en el base; overrides solo difieren en build/image, red y puertos | T1, T2, T3 |

## Test Scenarios

> Estos test scenarios validan la implementación de S-003. Al ser una story de orquestación, los inputs/outputs son **comandos `docker compose` + verificaciones HTTP/estructura de archivos**, con valores concretos. El agente developer los traduce a verificaciones manuales/scripts (no hay framework de tests unitarios para compose; la validación es por ejecución y por aserción estructural sobre los YAML).

| ID | Escenario | Input | Output Esperado | AC |
|---|---|---|---|---|
| TS-1 | Estructura de archivos creada | Listar `docker/` tras la implementación | Existen exactamente `docker/docker-compose.yml`, `docker/docker-compose.dev.yml`, `docker/docker-compose.prod.yml` y `docker/README.md` | CA-4 |
| TS-2 | Base es config válida y compartida | `docker compose -f docker/docker-compose.yml config` | Render exitoso; define servicios `atmosphere-api` y `atmosphere-web` con env vars comunes (`atmosphere-api`: `ENV`, `HTTP_ADDR`, `CORS_ALLOWED_ORIGINS`); SIN claves `build`, `image`, `ports`, ni red de entorno | CA-4 |
| TS-3 | Merge dev válido | `docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml config` | Render exitoso; `atmosphere-api` con `build.context: ../services/atmosphere-api` y `ports: "8080:8080"`; `atmosphere-web` con `build.context: ../services/atmosphere-app`, `build.args.API_BASE_URL: http://localhost:8080` y `ports: "8081:80"` | CA-1, CA-4 |
| TS-4 | Levantar dev end-to-end | `docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml up -d` y luego `curl -i http://localhost:8080/health` | API responde `200` con cuerpo `{ "status": "ok", "timestamp": "<RFC3339>" }` | CA-1 |
| TS-5 | Web sirve el bundle en dev | Con dev levantado, `curl -i http://localhost:8081/` | `200` con `Content-Type: text/html`; el HTML servido es el `index.html` del build de Flutter web | CA-1 |
| TS-6 | CORS permitido en dev | Con dev levantado, `curl -i -H "Origin: http://localhost:8081" http://localhost:8080/health` | `200` e incluye header `Access-Control-Allow-Origin: http://localhost:8081` y `Vary: Origin` | CA-2 |
| TS-7 | Preflight CORS permitido | `curl -i -X OPTIONS -H "Origin: http://localhost:8081" -H "Access-Control-Request-Method: GET" http://localhost:8080/health` | `204` con `Access-Control-Allow-Origin: http://localhost:8081`, `Access-Control-Allow-Methods: GET, POST, OPTIONS` y `Access-Control-Allow-Headers: Content-Type` | CA-2 |
| TS-8 | CORS denegado a origen ajeno | `curl -i -H "Origin: https://evil.example.com" http://localhost:8080/health` | `200` SIN header `Access-Control-Allow-Origin` (el browser bloquearía la lectura cross-origin) | CA-2 |
| TS-9 | Merge prod válido | `docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml config` | Render exitoso; `atmosphere-api` y `atmosphere-web` con `image:` (del registry, tag versionado), SIN `build`, SIN `ports`; ambos conectados a la red `ingress-network` declarada como `external: true` | CA-3, CA-4 |
| TS-10 | Vars de proxy en prod | Inspeccionar el render de prod (`config`) | `atmosphere-web` define `VIRTUAL_HOST`, `VIRTUAL_PORT: 80`, `LETSENCRYPT_HOST` (= `VIRTUAL_HOST`); `atmosphere-api` define `VIRTUAL_HOST`, `VIRTUAL_PORT: 8080`, `LETSENCRYPT_HOST` (= `VIRTUAL_HOST`); `atmosphere-api` con `ENV: production` y `CORS_ALLOWED_ORIGINS` no vacío | CA-3 |
| TS-11 | Prod no expone puertos al host | Render de prod (`config`) | Ningún servicio tiene clave `ports` | CA-3 |
| TS-12 | Prod referencia red externa, no la crea | Render de prod (`config`), sección `networks` | `ingress-network` declarada con `external: true` (compose NO la crea) | CA-3 |
| TS-13 | Paridad: sin duplicación de config común | Comparar los tres YAML | Nombres de servicio, env vars comunes y alias de red aparecen SOLO en el base; los overrides contienen exclusivamente `build`/`image`, `ports`/red y valores de env específicos del ambiente | CA-4 |
| TS-14 | Fail-fast de la API en prod sin CORS | Render/levantado de prod con `CORS_ALLOWED_ORIGINS` vacío para la API | El contenedor `atmosphere-api` falla al iniciar con error `config: CORS_ALLOWED_ORIGINS is required in production` (comportamiento del binario; el compose de prod DEBE proveer el valor para evitarlo) | CA-3 |

## Tasks

### Task 1: Crear `docker/docker-compose.yml` (base común)

**Status:** Completed

#### Description
Crear el archivo base que define la config **compartida** de ambos contenedores (`atmosphere-api` y `atmosphere-web`) — única fuente de verdad de lo común (ADR-004). El base NO define `build`, `image`, `ports` ni red de entorno; eso vive en los overrides. Es la pieza que garantiza la paridad estructural dev/prod.

#### Acceptance Criteria
1. Existe `docker/docker-compose.yml`.
2. Define dos servicios con nombres exactos: `atmosphere-api` y `atmosphere-web`.
3. `atmosphere-api` declara las env vars comunes con el contrato real del binario: `ENV`, `HTTP_ADDR` (no `PORT`), `CORS_ALLOWED_ORIGINS` (los valores por ambiente se inyectan en overrides; el base puede dejar `CORS_ALLOWED_ORIGINS` sin valor o referenciado por variable).
4. `atmosphere-web` no requiere env de runtime (su `API_BASE_URL` es build-arg); el base lo declara como servicio común sin `build`/`image`.
5. Define un alias de red común para que `atmosphere-web` pueda referirse a `atmosphere-api` por nombre de servicio si hace falta (sin declarar la red de entorno, que va en overrides).
6. NO contiene claves `build`, `image`, `ports`, ni definición de red específica de ambiente.
7. `healthcheck` de `atmosphere-api`: omitido/deshabilitado y documentado con comentario (la imagen distroless no tiene shell/curl; ver Architectural Context). Si se desea healthcheck del web, puede dejarse un comentario explicativo.
8. `docker compose -f docker/docker-compose.yml config` renderiza sin error.

#### Technical Details

**Architectural Context References:**
- Seguir las Implementation Rules de **ADR-004** (base = única fuente de verdad de lo común; sin build/image/ports/red de entorno).
- Usar el **contrato real** de env vars de `atmosphere-api` (`HTTP_ADDR`, no `PORT`) de la sección "Contrato real".
- Respetar **ADR-003** (stateless): sin servicio de DB ni volúmenes.

**Files to Create/Modify:**
- `docker/docker-compose.yml` (crear) — ruta relativa a la raíz del monorepo.

**Implementation Notes:**
- Nombre de servicio web = `atmosphere-web` (NO `atmosphere-app`).
- El base define la lista de servicios y las env vars **por nombre** (valores en overrides). Para `CORS_ALLOWED_ORIGINS` y `HTTP_ADDR` se recomienda interpolación (`${VAR}`) o dejar el valor en el override; mantener el base libre de valores específicos de ambiente.
- Documentar con comentario por qué el healthcheck de la API queda fuera (distroless sin shell).

**Dependencies:**
- Ninguna librería. Requiere Docker Compose v2.

#### Testing Requirements
**Unit Tests:** N/A (orquestación).
**Integration Tests:** TS-2 (render del base válido y sin claves de override).
**Manual Testing:** `docker compose -f docker/docker-compose.yml config` y revisar el render.

#### Task Dependencies
**Blocked by:** None
**Blocks:** T2, T3
**Can run in parallel with:** N/A

---

### Task 2: Crear `docker/docker-compose.dev.yml` (override local)

**Status:** Completed

#### Description
Crear el override de desarrollo que agrega `build:` desde el path del proyecto y expone puertos en localhost, para levantar el sistema vivo end-to-end en local con paridad respecto a prod. Solo debe contener las diferencias del ambiente dev (build, puertos, valores de env de dev), nunca duplicar lo común del base.

#### Acceptance Criteria
1. Existe `docker/docker-compose.dev.yml`.
2. `atmosphere-api`: `build.context: ../services/atmosphere-api`; `ports: "8080:8080"`; env de dev: `ENV: development`, `HTTP_ADDR: ":8080"`, `CORS_ALLOWED_ORIGINS: http://localhost:8081`.
3. `atmosphere-web`: `build.context: ../services/atmosphere-app`; `build.args.API_BASE_URL: http://localhost:8080`; `ports: "8081:80"`.
4. Red de entorno dev: red por defecto del proyecto (o una red dev declarada localmente), permitiendo localhost; NO referencia `ingress-network`.
5. NO redefine nombres de servicio ni env vars comunes ya presentes en el base más allá de los valores específicos de dev.
6. `docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml config` renderiza sin error y el merge refleja build + ports.

#### Technical Details

**Architectural Context References:**
- ADR-004: override dev MUST agregar `build:` desde el path y exponer puertos en localhost (API 8080, web 8081).
- `API_BASE_URL` es **build-arg** del web (sección "Contrato real" — constraint clave): va bajo `build.args`, no como env de runtime.
- CORS dev: `CORS_ALLOWED_ORIGINS: http://localhost:8081` para que el web (origen `:8081`) sea autorizado por la API.

**Files to Create/Modify:**
- `docker/docker-compose.dev.yml` (crear) — relativa a la raíz del monorepo.

**Implementation Notes:**
- `build.context` relativo a `docker/`: `../services/atmosphere-api`, `../services/atmosphere-app`.
- Mapeo de puertos: API `8080:8080` (host:contenedor), web `8081:80` (host:contenedor nginx).
- El `API_BASE_URL` dev apunta al host (`http://localhost:8080`) porque el browser del usuario resuelve la llamada (la web Flutter corre en el browser, no server-side), por lo que debe ser una URL alcanzable desde el host, no el nombre de servicio interno.

**Dependencies:**
- Dockerfiles existentes de ambos servicios (S-001, S-002) — sin cambios.

#### Testing Requirements
**Unit Tests:** N/A.
**Integration Tests:** TS-3 (merge dev), TS-4 (`/health` 200), TS-5 (web sirve index), TS-6/TS-7/TS-8 (CORS).
**Manual Testing:** `docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml up -d`; `curl http://localhost:8080/health`; abrir `http://localhost:8081`.

#### Task Dependencies
**Blocked by:** T1
**Blocks:** T5
**Can run in parallel with:** T3

---

### Task 3: Crear `docker/docker-compose.prod.yml` (override producción)

**Status:** Completed

#### Description
Crear el override de producción que usa `image:` del registry oficial (tags versionados), conecta los servicios a la red externa `ingress-network`, NO expone puertos al host, y declara las variables del proxy (`VIRTUAL_HOST`, `VIRTUAL_PORT`, `LETSENCRYPT_HOST`) por servicio expuesto. Solo contiene las diferencias de prod respecto al base.

#### Acceptance Criteria
1. Existe `docker/docker-compose.prod.yml`.
2. `atmosphere-api` y `atmosphere-web` usan `image:` (registry oficial, tag versionado parametrizable, p. ej. `${API_IMAGE}` / `${WEB_IMAGE}`); SIN `build`.
3. Ningún servicio define `ports`.
4. Ambos servicios se conectan a la red `ingress-network`, declarada en la sección `networks` como `external: true` (el compose NO la crea).
5. `atmosphere-api`: `ENV: production`, `CORS_ALLOWED_ORIGINS` con el/los dominio(s) del frontend (no vacío — evita el fail-fast), `VIRTUAL_HOST` (dominio API), `VIRTUAL_PORT: 8080`, `LETSENCRYPT_HOST` (= `VIRTUAL_HOST`).
6. `atmosphere-web`: `VIRTUAL_HOST` (dominio frontend), `VIRTUAL_PORT: 80`, `LETSENCRYPT_HOST` (= `VIRTUAL_HOST`).
7. `docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml config` renderiza sin error (con las variables de entorno requeridas definidas o con placeholders documentados).

#### Technical Details

**Architectural Context References:**
- ADR-004: override prod MUST usar `image:` del registry, red externa `ingress-network`, sin `ports`; cada servicio expuesto define `VIRTUAL_HOST`/`VIRTUAL_PORT`/`LETSENCRYPT_HOST`.
- Integration Points: `ingress-network` (external), nginx-proxy + acme-companion, registry oficial.
- Constraint `API_BASE_URL` build-time: la imagen `atmosphere-web` del registry ya fue construida con el `API_BASE_URL` productivo; el compose de prod NO lo inyecta en runtime (documentar en README, T4).
- Fail-fast (TS-14): `ENV: production` exige `CORS_ALLOWED_ORIGINS` no vacío en la API.

**Files to Create/Modify:**
- `docker/docker-compose.prod.yml` (crear) — relativa a la raíz del monorepo.

**Implementation Notes:**
- `VIRTUAL_PORT` apunta al puerto **interno** del contenedor: API `8080`, web `80`.
- Tags/repos de imagen parametrizados por variables (`API_IMAGE`, `WEB_IMAGE`) para no hardcodear el registry; documentar los nombres esperados en el README.
- `networks: { ingress-network: { external: true } }`.

**Dependencies:**
- Imágenes publicadas en el registry (prerequisito operativo, fuera del alcance de creación de archivos).

#### Testing Requirements
**Unit Tests:** N/A.
**Integration Tests:** TS-9 (merge prod: image, sin build/ports, red externa), TS-10 (vars proxy), TS-11 (sin ports), TS-12 (red external), TS-14 (fail-fast documentado).
**Manual Testing:** `docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml config` con variables/placeholders; revisar render. (No se levanta realmente sin `ingress-network` y proxy.)

#### Task Dependencies
**Blocked by:** T1
**Blocks:** T4
**Can run in parallel with:** T2

---

### Task 4: Crear `docker/README.md` (prerequisitos + comandos)

**Status:** Completed

#### Description
Documentar cómo usar la orquestación: comandos dev/prod combinando base + override, prerequisitos de producción (red externa `ingress-network` + proxy nginx-proxy/acme-companion, imágenes publicadas con el `API_BASE_URL` productivo horneado), las variables de entorno requeridas por ambiente y el contrato real de la API (`HTTP_ADDR`). Mitiga los riesgos del ADR-004 (drift dev/prod e `ingress-network` inexistente).

#### Acceptance Criteria
1. Existe `docker/README.md`.
2. Incluye los comandos exactos: dev `docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml up`; prod `docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml up -d`.
3. Documenta el prerequisito de prod: la red externa `ingress-network` debe existir en el host (`docker network create ingress-network` si no existe) y el proxy (nginx-proxy + acme-companion) debe estar corriendo en ella.
4. Documenta que `atmosphere-web` hornea `API_BASE_URL` en build-time: en prod la imagen del registry debe haberse construido con el dominio productivo de la API.
5. Documenta las variables por ambiente, incluyendo la aclaración `HTTP_ADDR` (no `PORT`) para la API y el fail-fast de `CORS_ALLOWED_ORIGINS` en producción.
6. Documenta los puertos: dev API `8080`, web `8081`; prod sin puertos al host (entra por el proxy).

#### Technical Details

**Architectural Context References:**
- ADR-004 Risks: documentar prerequisito de `ingress-network`/proxy y la mitigación del drift (común en el base).
- "Contrato real" + "DISCREPANCIA DOCUMENTADA": dejar claro `HTTP_ADDR` vs `PORT`.
- Constraint `API_BASE_URL` build-time para prod.

**Files to Create/Modify:**
- `docker/README.md` (crear).

**Implementation Notes:**
- Tabla de variables por ambiente para ambos servicios.
- Nota explícita de que los targets desktop (Windows/Linux) NO se contenerizan (ADR-004).

**Dependencies:**
- Ninguna.

#### Testing Requirements
**Unit Tests:** N/A.
**Integration Tests:** TS-1 (README presente junto a los tres compose).
**Manual Testing:** Revisar que los comandos del README ejecutan tal cual contra los archivos creados.

#### Task Dependencies
**Blocked by:** T3
**Blocks:** None
**Can run in parallel with:** T5

---

### Task 5: Verificación end-to-end del sistema vivo en dev

**Status:** Completed

#### Description
Levantar el sistema completo con los compose dev y validar el sistema vivo end-to-end: la API responde `/health`, el web sirve el bundle, y el CORS funciona cross-origin entre el web (`:8081`) y la API (`:8080`). Cierra CA-1 y CA-2 con evidencia real de ejecución.

#### Acceptance Criteria
1. `docker compose -f docker/docker-compose.yml -f docker/docker-compose.dev.yml up -d` levanta ambos contenedores sin error.
2. `GET http://localhost:8080/health` → `200` con `{ "status": "ok", "timestamp": "<RFC3339>" }`.
3. `GET http://localhost:8081/` → `200` HTML (index del build Flutter web).
4. `GET http://localhost:8080/health` con `Origin: http://localhost:8081` → incluye `Access-Control-Allow-Origin: http://localhost:8081`.
5. Preflight `OPTIONS` desde `http://localhost:8081` → `204` con los headers CORS correctos.
6. Origen ajeno (`https://evil.example.com`) → respuesta `200` SIN `Access-Control-Allow-Origin`.

#### Technical Details

**Architectural Context References:**
- API Context (`GET /health`), contrato CORS implementado (sección "Contrato real").
- Flow Context (escenario "Levantar el sistema en dev").

**Files to Create/Modify:**
- Ninguno (tarea de verificación). Si se detectan defectos, se corrigen en T1/T2.

**Implementation Notes:**
- Verificar con `curl -i` los headers CORS exactos.
- Si el web no alcanza la API, revisar `API_BASE_URL` build-arg (debe ser `http://localhost:8080`, alcanzable desde el browser del host).

**Dependencies:**
- Docker + Docker Compose v2 operativos en el host.

#### Testing Requirements
**Unit Tests:** N/A.
**Integration Tests:** TS-4, TS-5, TS-6, TS-7, TS-8.
**Manual Testing:** Secuencia de `curl` arriba + abrir `http://localhost:8081` en un browser.

#### Task Dependencies
**Blocked by:** T2
**Blocks:** None
**Can run in parallel with:** T4

---
