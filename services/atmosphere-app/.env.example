# Configuración de entorno de atmosphere-app.
# Copiar a `.env` (git-ignored) y ajustar los valores.

# Base URL de atmosphere-api (sin barra final). Requerida (fail-fast si falta).
API_BASE_URL=http://localhost:8080
