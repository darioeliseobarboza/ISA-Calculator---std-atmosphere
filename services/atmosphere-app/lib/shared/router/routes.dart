/// Paths de la app como constantes (convención `navigation`). Los widgets y el
/// router referencian estas constantes, nunca strings literales.
abstract final class Routes {
  /// Ruta raíz. Monta la `CalculatorScreen` (vista principal de la app).
  static const String root = '/';
}
