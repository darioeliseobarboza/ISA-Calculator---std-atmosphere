// Re-export del `envProvider` para descubrirlo desde `shared/providers/`
// (la definición vive junto al tipo en `shared/config/env.dart`).
export 'package:atmosphere_app/shared/config/env.dart' show Env, envProvider;
